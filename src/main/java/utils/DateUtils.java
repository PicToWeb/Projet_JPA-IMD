package utils;

import java.time.LocalDate;

public class DateUtils {
	
	public static LocalDate stringToDate(String date) {
		String chaine = date.trim();
		String[]tokens = chaine.split(" ");
		
		if (chaine.split(" ").length == 3)
			
		return dateModif;
	}

}
